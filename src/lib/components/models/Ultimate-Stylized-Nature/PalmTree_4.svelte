<!--
Auto-generated by: https://github.com/threlte/threlte/tree/main/packages/gltf
Command: npx @threlte/gltf@1.0.0-next.13 C:\Users\Aaron\Documents\FunBit\static\models\PalmTree_4.gltf --root /models/ --types --printwidth 120 --precision 2
-->

<script lang="ts">
	import * as THREE from 'three';
	import { Group } from 'three';
	import { T, type Props, type Events, type Slots, forwardEventHandlers } from '@threlte/core';
	import { useGltf, useTexture } from '@threlte/extras';

	type $$Props = Props<THREE.Group>;
	type $$Events = Events<THREE.Group>;
	type $$Slots = Slots<THREE.Group> & { fallback: {}; error: { error: any } };

	export const ref = new Group();

	type GLTFResult = {
		nodes: {
			Cube004: THREE.Mesh;
			Cube004_1: THREE.Mesh;
		};
		materials: {
			PalmTree_Trunk: THREE.MeshStandardMaterial;
			PalmTree_Leaves: THREE.MeshStandardMaterial;
		};
	};

	const gltf = useGltf<GLTFResult>('/Ultimate-Stylized-Nature/PalmTree_4.gltf');
	const texture1 = useTexture('/Ultimate-Stylized-Nature/Textures/PalmTree_Trunk.png');
	const normalMap1 = useTexture('/Ultimate-Stylized-Nature/Textures/PalmTree_Trunk_Normal.png');
	const texture2 = useTexture('/Ultimate-Stylized-Nature/Textures/PalmTree_Leaves.png');

	const assets = Promise.all([gltf, texture1, normalMap1, texture2]);

	const component = forwardEventHandlers();
</script>

<T is={ref} dispose={false} {...$$restProps} bind:this={$component}>
	{#await assets}
		<slot name="fallback" />
	{:then [gltf, t1, n1, t2]}
		<T.Mesh geometry={gltf.nodes.Cube004.geometry}>
			<T.MeshStandardMaterial
				map={t1}
				map.wrapS={THREE.RepeatWrapping}
				map.wrapT={THREE.RepeatWrapping}
				normalMap={n1}
				normalMap.wrapS={THREE.RepeatWrapping}
				normalMap.wrapT={THREE.RepeatWrapping} />
		</T.Mesh>
		<T.Mesh geometry={gltf.nodes.Cube004_1.geometry}>
			<T.MeshStandardMaterial map={t2} map.flipY={false} side={THREE.DoubleSide} alphaTest={0.5} />
		</T.Mesh>
	{:catch error}
		<slot name="error" {error} />
	{/await}

	<slot {ref} />
</T>
